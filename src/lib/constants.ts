
export const API_URL = 'http://localhost:5000';

// Roles
export const ROLES = {
  USER: 'user',
  ADMIN: 'admin'
};

// Reservation statuses
export const RESERVATION_STATUS = {
  PENDING: 'pending',
  CONFIRMED: 'confirmed',
  CANCELLED: 'cancelled'
};
